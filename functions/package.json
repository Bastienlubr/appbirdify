{
  "name": "appbirdify-functions",
  "private": true,
  "engines": {
    "node": "20"
  },
  "type": "commonjs",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rimraf lib",
    "deploy": "npm run build && firebase deploy --only functions",
    "watch": "tsc -w -p tsconfig.json",
    "migrate:v3": "node lib/tools/migrate_abonnement_v3.js"
  },
  "dependencies": {
    "appbirdify": "file:..",
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^5.1.0",
    "googleapis": "^135.0.0",
    "stripe": "^14.0.0",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "rimraf": "^5.0.5",
    "typescript": "^5.5.4"
  }
}
